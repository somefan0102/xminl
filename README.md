# xminl

**xminl** (**X**ML **Min**imal **L**exer) is a small XML 1.0 lexical analyzer API, written in ANSI C.

## Usage

TODO

## Limitations

- Not a fully-fledged XML validator, parser, or reader API
- DOCTYPE declarations and DTD lexing are not supported